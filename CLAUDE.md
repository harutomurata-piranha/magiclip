# MagiClip

**コンセプト: 魔法のように動画をつくれる。**
動画をアップロードするだけで、字幕つきの縦型ショート動画が完成する。

## UI を書く前に必ず読む

**`docs/design-system.md` を読んでから UI のコードを書くこと。**
色・余白・文字サイズ・角丸・影は、そこに定義されたトークンだけを使う。
定義にない値をハードコードしない。テンプレートにインライン `style` を書かない。

特に守ること（詳細は設計書）:

- 装飾的な絵文字を使わない。グラデーションを使わない。影を常用しない
- アクセントは 1 色（`--accent`）。ブランドの紫はロゴ・ファビコン・透かし・**完成の瞬間**のみ
- 1 画面に主 CTA は 1 つ。見せ場も 1 つ
- 文言は指示に徹する。感嘆符・煽り・「〜しましょう!」を書かない
- 空 / 処理中 / エラー / 完了の 4 状態を設計してから実装する

## 構成

| 役割 | ファイル |
|---|---|
| ビジネス本体（箱） | `server.py` |
| AI 編集エンジン | `app.py`（本体） / `video_processor.py`（差し替え層） |
| 設定 / データ | `config.py` / `models.py` |
| 画面 | `templates/` / `static/style.css` |
| デザイン基準 | **`docs/design-system.md`** |

## 作業ルール

- 機能実装と UI 改善を同じコミットに混ぜない。1 コミット 1 目的
- 既存の機能を壊さない。挙動を変える必要があるときは先に申告する
- 判断に迷ったら実装せずに質問する
